/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'source.unsplash.com', 'via.placeholder.com', 'placehold.co', 'dummyimage.com'],
      },
};

export default nextConfig;
